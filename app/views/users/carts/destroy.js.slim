| $("#deleteForm#{current_person("user").id}").html(`
= render "users/carts/cart_table", cart_items: @cart.cart_items
| `);

| $("#QuantityIconCartForm").html(`
= render "users/shared/quantity_icon"
| `);
