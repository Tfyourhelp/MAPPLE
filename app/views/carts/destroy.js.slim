/-if @cart.cart_items.length == 0
  window.location.replace("#{carts_path}");
/carts_path

| $("#deleteForm#{current_person("user").id}").html(`
= render "cart_table", cart_items: @cart.cart_items
| `);

| $("#QuantityIconCartForm").html(`
= render "shared/quantity_icon"
| `);


/ | $("#changequantityForm#{@cart_item&.id}").html(`
/ = render "change_quantity_cart_item", cart_item: @cart_item,  cart_items_length: @cart.cart_items.length , action: "delete"
/ | `);

/ | $("#yourbillForm").html(`
/ = render "your_bill", cart_items: @cart.cart_items
/ | `);